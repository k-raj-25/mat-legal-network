# Application
SECRET_KEY=change-me-in-production
FLASK_ENV=development
FLASK_DEBUG=1

# Database (local / Docker Compose)
POSTGRES_USER=mat
POSTGRES_PASSWORD=mat
POSTGRES_DB=mat_legal
DATABASE_URL=postgresql+psycopg://mat:mat@db:5432/mat_legal

# Local (outside Docker) example:
# DATABASE_URL=postgresql+psycopg://mat:mat@localhost:5432/mat_legal

# Mail (contact form). Use a Gmail app password, not the account password.
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=rajput.kamal25@gmail.com
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=rajput.kamal25@gmail.com
CONTACT_RECIPIENT=rajput.kamal25@gmail.com
